// Esqueleto: preenchido numa tarefa futura desta mesma entrega. O compositor
// (montar.js) já carrega este módulo; entra() falso não acrescenta nada ao
// prompt até a tarefa que implementa o fluxo "limite-perguntas" substituir
// este arquivo.
module.exports = {
  nome: 'limite-perguntas',
  entra() { return false; },
  linhas() { return []; },
};
