// Esqueleto: preenchido numa tarefa futura desta mesma entrega. O compositor
// (montar.js) já carrega este módulo; entra() falso não acrescenta nada ao
// prompt até a tarefa que implementa o fluxo "suporte-diagnostico" substituir
// este arquivo.
module.exports = {
  nome: 'suporte-diagnostico',
  entra() { return false; },
  linhas() { return []; },
};
